import React from 'react';

import classes from './right-section.scss';

const RightSection = () => {
    return (
        <section className={classes.right}>
            <figure className={classes.banner}></figure>
        </section>
    );
}

export default RightSection;

